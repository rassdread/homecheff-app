// (géén "use client" hier)
import { ReactNode } from "react";

export const metadata = {
  title: "Profiel | HomeCheff",
};

export default function ProfileLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
