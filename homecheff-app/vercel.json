{
	"buildCommand": "npx prisma generate && SKIP_ENV_VALIDATION=true npm run build",
	"installCommand": "npm ci",
	"outputDirectory": ".next",
	"framework": "nextjs",
	"env": {
		"NODE_ENV": "production"
	}
}
