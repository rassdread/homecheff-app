{
	"buildCommand": "rm -rf .next node_modules package-lock.json && npm install && npx prisma generate && npm run build",
	"installCommand": "npm install",
	"outputDirectory": ".next",
	"framework": "nextjs",
	"env": {
		"NODE_ENV": "production"
	},
	"functions": {
		"app/api/**/*.ts": {
			"maxDuration": 30
		}
	}
}
