// --- BEGIN PROFILE PHOTOS PRISMA PATCH ---
// Plak onderstaande velden in je bestaande modellen en migreer.
// In User:
/*
model User {
  id           String   @id @default(uuid())
  email        String   @unique
  passwordHash String?
  role         UserRole @default(BUYER)
  name         String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  // nieuw:
  bio          String?
  interests    String[]  @default([])
  profileImage String?
}
*/

// In Company (voor sellers):
/*
model Company {
  id              String   @id @default(uuid())
  user            User     @relation(fields: [userId], references: [id])
  userId          String   @unique
  name            String
  // ... overige velden ...
  updatedAt       DateTime @updatedAt

  // nieuw:
  workplacePhotos String[] @default([])
}
*/
// Draai daarna:
// npx prisma migrate dev -n "profile_photo_fields"
// npx prisma generate
// --- END PROFILE PHOTOS PRISMA PATCH ---
