"use client";

/**
 * Diagnostics host seam — Phase 2B/2C expose diagnostics via data-* attributes
 * on SettingsWorkspaceShadowRoot (including chrome occupancy). No user-facing UI.
 */
export type { SettingsShadowDiagnostics } from "@/lib/adaptive-workspace-react";
export type { WorkspaceChromeOccupancy } from "@/lib/adaptive-workspace-react";
