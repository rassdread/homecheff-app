"use client";

/**
 * Notifications shadow diagnostics seam — Phase 2D.
 * Diagnostics live on SettingsWorkspaceShadowRoot data-aw-notifications-* attrs.
 * No panel renderer. No Domain State. No open/close.
 */
export type {
  NotificationsPresentationIntent,
  NotificationsShadowDiagnostics,
} from "@/lib/adaptive-workspace-react";
