# Marketplace order fee snapshot (HomeCheff)

`Order.hcFeeSnapshot` JSON copy of Growth `MarketplaceOrderFeeSnapshot` when fee engine is live. Unique one per order. No client-authored fee. No historical backfill.
