# GPS Proof

**Code / validators:** PASS (`test:gps-location-repair` 30/30).

**Live device GPS journey:** NOT EXECUTED.

Production contains repaired GPS paths. Operator must confirm allow → coords → reverse geocode → nearby → saved preference, and deny → manual fallback.
