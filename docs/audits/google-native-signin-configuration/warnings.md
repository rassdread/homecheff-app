# Warnings

1. Real Android Google proof not run (no deploy).
2. `NEXT_PUBLIC_GOOGLE_NATIVE_CLIENT_ID` still absent in Vercel — legacy public id must inline after redeploy.
3. Development missing `GOOGLE_CLIENT_ID`.
4. System-browser fallback depends on Custom Tabs cookie behaviour — verify on device.
5. iOS native not in scope.
