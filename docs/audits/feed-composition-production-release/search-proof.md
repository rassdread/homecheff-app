# Search Review

Validators: search applies to `filteredApiInspiration` / `filteredFeedInspiration` and sales.

Production probes (homecheff.eu, live data):
- `q=cake` national → DISH hit (Inspiration searchable)
- `q=sushi` nearby Rotterdam → 0 exact (continuity path intended post-deploy)
- Vertical GROWN → DISH items returned

Post-deploy UI search journeys: Chrome headless bounded; Safari/Edge/Android interactive not proven.
