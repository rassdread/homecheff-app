# Accessibility Review

- Visible `common.place` label on sidebar + mobile sheet.
- `aria-label` on place inputs.
- `data-testid="feed-place-input"`.
- Enter submits; Escape closes mobile sheet.
- Discovery filters toggle exposes `aria-expanded`.
- Mobile sheet prefers place focus when opened for choose-place (not trapped on close).
- Touch targets: existing py-2 / py-2.5 controls retained.
