# GPS-to-Feed Proof

## Code path (PASS)

Tap → permission/request → coords → `userLocation` + `locationSource=gps` + Nearby + distance sort → preference → feed effect deps refresh.

Denial → message + `requestPlaceInputFocus` / mobile sheet → manual place/postcode.

## Live Production / Android device

Not completed in this agent session (bot protection / no device). Operator proof required after deploy.
