# Exact Root Cause

**Single blocking layer:** place input unmounted behind collapsed Discovery Filters + choose-place focus targeting a null ref.

Contributing mobile issue: sheet focus trap landed on close control instead of place field.

Not GPS. Not geocoder billing. Not auth. Not `pointer-events: none` on a visible input.
