# Remaining Warnings (non-blocking)

1. Cookie/privacy modal can own first phone paint until dismissed (P2).
2. Guest surfaces still expose Verdienen / “Werken bij” (P2 identity).
3. Secondary EN “listing” strings may remain outside Workspace primary copy (out of scope).
4. CSP blocks Google Tag Manager `gtag` script (pre-existing console noise).
5. Dual-rail layouts may show one demoted secondary Create alongside one primary (intentional hierarchy).
6. Landscape work-compact hides nearby scope chips until Filters; nearby remains the applied default.
7. Location: after “Use my location”, a loading affordance may linger briefly while place text is already present — not a dead control; monitor in real-user sessions.
8. Gated guest routes may render HTTP 200 with in-page auth gates rather than hard redirects (existing pattern).
