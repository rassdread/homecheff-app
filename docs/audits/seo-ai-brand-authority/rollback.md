# Rollback

```bash
git revert <commit-A>   # or reset feature branch before merge
```

Scope: `lib/seo` content SSOTs + FAQ JSON-LD content + evidence docs.  
No DB/migrations. No AW/planner/routing. Low risk.
